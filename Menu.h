#include <stdint.h>





extern uint8_t getNextMenu(uint8_t current);

extern uint8_t getPreviousMenu(uint8_t current);

extern void drawSelectedMenu(uint8_t current);

extern void selectMenu(uint8_t current);

extern void removeIconsMenu();

